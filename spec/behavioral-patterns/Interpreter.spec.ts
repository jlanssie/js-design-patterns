describe('Interpreter', () => {
  test('An Interpreter accepts a context. It forwards the context to a terminal expression or a non-terminal expression, which in turn functions as an interpreter that either forwards the context further to a terminal expression or another non-terminal expression.', () => {
    // const context
    // const interpreter
    // interpreter.interpret(context);
  });
});
